# JavaScript 学习笔记

## 闭包
闭包是指函数能记住并访问它创建时的词法作用域，即使这个函数在它的词法作用域之外执行。

## Promise
Promise 是 JavaScript 中处理异步操作的对象，有三种状态：pending、fulfilled、rejected。

## async/await
async/await 是 Promise 的语法糖，让异步代码看起来像同步代码。